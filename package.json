{
  "name": "cabinet-quoting-tool",
  "version": "1.0.0",
  "private": true,
  "description": "Cabinet quoting tool for installers",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend",
    "build:backend": "npm install --prefix backend && npm run build --prefix backend",
    "db:generate": "npx prisma generate --schema=backend/prisma/schema.prisma",
    "build": "npm install --prefix frontend && ./frontend/node_modules/.bin/tsc -p frontend && npm install --prefix backend && ./backend/node_modules/.bin/tsc -p backend",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
